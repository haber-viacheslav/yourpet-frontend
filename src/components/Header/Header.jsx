import { HeaderWrapper } from './Header.styled';
import { NavBar } from "../Navigation/NavBar/NavBar";

export const Header = () => {
  return (
    <HeaderWrapper>
        <NavBar />
    </HeaderWrapper>
  );
};
